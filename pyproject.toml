[project]
name = "axon-lsp"
version = "0.1.8"
requires-python = ">=3.8"
dependencies = [
    "pygls>=1.2,<2.0.0",
    "lsprotocol>=2023.0.0",
    "dotenv>=0.9.9",
]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
    "pytest-mock>=3.14.1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
